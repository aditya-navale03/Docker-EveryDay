# 1. docker pull IMAGE_NAME:version
--> to install any version of image

# 2. docker run -d IMAGE_NAME
--> to run in detach mode
--> we can say we can run in background

## by default containers run in attach mode

# 3. docker run --name COUNT_NAME -d IMAGE_NAME
--> to give specific names to container